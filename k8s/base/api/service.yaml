apiVersion: v1
kind: Service
metadata:
  name: quorum-api
  namespace: quorum
  labels:
    app.kubernetes.io/name: quorum-api
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: quorum
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: quorum-api
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
